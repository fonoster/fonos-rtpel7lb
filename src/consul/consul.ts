// import Consul from 'consul'
// const consul = new Consul();

export const listServices = (err: any, result: any) => {
  if (err) throw err
  return result
}

// consul.agent.service.list(listServices);
